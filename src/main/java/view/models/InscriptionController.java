package view.models;

public class InscriptionController {
    private InscriptionView view;
    private InscriptionModel model;

    public InscriptionController(InscriptionView view, InscriptionModel model) {
        this.view = view;
        this.model = model;

       // view.addSendButtonListener(new SendButtonListener());
        //view.addDefilementButtonListener(new DefilementButtonListener());
        //view.addChangeButtonListener(new ChangeButtonListener());
    }

    public class ChangeButtonListener {
    }

    public class send {
    }
}

